package loadero.model;

/**
 * Enum types are used for creation/validation of concrete LoaderoModel classes.
 */
public enum LoaderoType {
    LOADERO_TEST_OPTIONS,
    LOADERO_GROUP,
    LOADERO_PARTICIPANT,
    LOADERO_RUN_INFO,
    LOADERO_TEST_RUN_PARTICIPANT_RESULT,
    LOADERO_SCRIPT_FILE_LOC,
    LOADERO_RUN_RESULT
}
